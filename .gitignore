node_modules/
dist/
# Generated by scripts/build-meta.mjs on every build (buildTime churns); the
# deployed copy always comes from the build output, never from git.
public/version.json
.DS_Store
sources/
*.log
.orchestration/

# Local-only ML/dataset artifacts — never commit user audio or raw recordings.
datasets/
recordings/
*.wav
*.flac
*.aiff
*.aif
*.mp3
*.ogg
*.m4a
scripts/dataset-out/
!scripts/fixtures/
